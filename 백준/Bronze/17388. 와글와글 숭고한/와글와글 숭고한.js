const input = require("fs").readFileSync("/dev/stdin").toString().trim().split(' ').map(Number);
console.log(input.reduce((a,b)=>a+b) >= 100 ? 'OK' : ['Soongsil','Korea','Hanyang'][[input.indexOf(Math.min(...input))]]);